// finder — УНИВЕРСАЛЬНАЯ нода-источник (плагин). Следит за своей папкой и выдаёт ОДИН файл
// за интервал (по Order). Конфиг: folder/searchType/order/interval/days/window/deleteAfter.
//
// Processing-функции у ноды нет: это ИСТОЧНИК (корень пайплайна, queue[0] в processItem) —
// его output ставит драйвер постинга (выбранный файл), сам плагин в графе не исполняется.
// Файл существует только потому, что загрузчик плагинов требует непустой "main" — заглушка.
