// Шаблон entry-файла для AE (скопируй в jsx/dev/<name>.ts).
//
// Правила:
//  • ровно ОДНА exported-функция (имя любое) — это и есть точка входа;
//  • `var inObj = {}` объявляй ВНУТРИ функции: это якорь, плагин подставит сюда
//    реальные аргументы во время обработки, и его же ты используешь в коде;
//  • вызов писать НЕ нужно — билд снимет export и сам допишет `myFn();` с маркером.
//    (Если очень нужны аргументы в вызове — напиши вызов последней строкой, напр.
//     `myFn(42)`, билд сохранит его вместе с аргументами.)
//  • код в ES3-стиле: var, обычные function(), без `=>`/let/const/`` ` ``-строк.
//    TS-типы (`: any`) писать можно — esbuild их снимает.

export function myFn() {
	var inObj = {}; // ← плагин заменит на реальные аргументы

	// ... твой ES3-код, читаешь данные из inObj.*

	return []; // что вернёшь — попадёт в result.json (data)
}
