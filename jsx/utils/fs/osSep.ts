// ====================================================================
// функция возвращает нужный разделитель в зависимости от системы
// ====================================================================
export function osSep() {
	if ($.os.match(/Windows/)) {
		return '\\';
	} else if ($.os.match(/Mac/)) {
		return '/';
	} else {
		return '/';
	}
}
